/**
 * @deprecated Use `src/api/client.ts` instead. This module is kept only for
 * backward-compatibility references. All new code should import from `../api/client`.
 */
import client from '../api/client';

export default client;
